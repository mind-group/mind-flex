import React                from 'react'
import Flex                 from '../components/Flex'

export default ({ theme }) => (
  <div className={ theme.container }>
    <Flex>
      <div className={ theme.box }>1</div>
      <div className={ theme.box }>2</div>
      <div className={ theme.box }>3</div>
    </Flex>
  </div>
)
